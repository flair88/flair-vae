    <h1 id="logo">
      <a href="/" title="Flair" class="logo_image home">Flair</a>
      <?php /* $locations = vae("/locations"); ?>
      <? if ($locations->other_locations->count): ?>
        <div class="cities">
          <? $i = 1; ?>
          <? foreach ($locations->get("other_locations", array('limit'=>3)) as $city): ?>
            <? if ($i==2): ?>
              <p class="bullet">&bull;</p>
            <? endif; ?>
              <a class="home" data-id="<? echo $city->id; ?>"><? echo $city->title; ?></a>
            <? if ($i==2): ?>
              <p class="bullet">&bull;</p>
            <? endif; ?>
            <? $i++; ?>
          <? endforeach; ?>
        </div>

    </h1>
    <div class="addresses">
      <? foreach ($locations->get("other_locations", array('limit'=>3)) as $city): ?>
        <div data-id="<? echo $city->id; ?>">
          <p><h2>Flair <? echo $city->title; ?></h2><? echo $city->address; ?></br>Tel: <a href="tel:<? echo $city->phone; ?>"><? echo $city->phone; ?></a></br><a href="<? echo $city->url; ?>"><? echo $city->display_url; ?></a></p></div>
      <? endforeach; ?>
    </div>
    <? endif; */?>
    </h1>